"use strict";
const express = require("express");
const {createAvailabilityService,renderPage} = require("./aiAvailability");
const availability = createAvailabilityService({db:require("./db"),businessManager:require("./businessManager"),matchesMarketplaceMetro:require("./marketplaceMetros").matchesMarketplaceMetro});
const router = express.Router();
const method = `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Appointment data coverage and methodology | NextAppt.ai</title><link rel="canonical" href="https://nextappt.ai/availability-methodology"></head><body><main><h1>NextAppt appointment data: coverage and methodology</h1><p>NextAppt aggregates appointment openings for configured services at publicly listed businesses in the Austin area. Coverage is partial: it is not a list of every Austin business, service or appointment. An empty result means no matching inventory was returned, not that the area has no openings.</p><h2>Source and freshness</h2><p>Confirmed means an opening was observed in a business booking source; it does not mean reserved or guaranteed. Last checked is the observation timestamp retained for that exact confirmed entry. Fresh means checked within 30 minutes. Older observations are stale; missing or invalid timestamps are unknown. Inventory-read and page-generation times do not establish booking-source freshness. Refresh schedules and service windows vary.</p><p>Inferred means a possible service opening derived from other availability, not an independently observed opening for that service. Manual means entered by an administrator, not independently checked by this feed. The default feed includes only fresh confirmed entries. Business verification describes the business listing and is independent of whether an appointment was confirmed.</p><h2>Reading the inventory</h2><p><a href="/availability/austin">Browse Austin inventory in HTML</a> or use <a href="/api/public/availability">the public JSON feed</a>. Both use the same data, require no login or JavaScript, and do not trigger scraping or booking. <a href="/api/public/availability?includeStale=true&amp;includeInferred=true">Include stale, unknown, manual and inferred entries</a> only when you need those explicitly labelled records.</p><p>Default window: the next 24 hours. At most seven days per request, within the next eight days. All Austin wall-clock times use America/Chicago, including daylight saving changes. startAt is UTC; localStart includes the Austin UTC offset. Nonexistent or ambiguous local times are excluded instead of guessed.</p><h2>Parameters</h2><dl><dt>category</dt><dd>Exact configured category slug, for example massage.</dd><dt>business</dt><dd>Exact business slug or name.</dd><dt>duration</dt><dd>Service duration in minutes (1â€“480).</dd><dt>hours</dt><dd>1â€“168; default 24. Use instead of date or from/to.</dd><dt>date</dt><dd>Austin calendar date YYYY-MM-DD; past hours excluded.</dd><dt>from and to</dt><dd>Both ISO timestamps with Z or an explicit offset. End exclusive. Past hours excluded.</dd><dt>lat, lon and radiusKm</dt><dd>All three required together; radius greater than zero, at most 100 km. Entries lacking coordinates are excluded for this filter.</dd><dt>includeInferred</dt><dd>true or false; default false.</dd><dt>includeStale</dt><dd>true or false; default false. Must be true to retrieve unknown-freshness inferred/manual entries.</dd><dt>page</dt><dd>Page number, starting at 1; 100 entries per page. Follow next until null.</dd></dl><p>totalMatching describes all matching eligible entries in the current read, not just one page. Publicly hidden inventory and disabled businesses/services are excluded. Card-display limits do not truncate this inventory feed. The dataset may be cached for up to 15 seconds. Live inventory can change between pages; this is not an immutable export.</p><p>Unknown service mappings, invalid booking links and unsafe timestamps are excluded and counted at dataset scope. If the bounded dataset exceeds 20,000 rows or the database cannot be read, the endpoint returns 503 rather than a misleading empty or partial success. No customer or owner contact data, raw scraper payloads or billing fields are included.</p><p>Always follow the business booking link to reconfirm and complete a reservation. Cite the NextAppt results URL and source-check time when using the data.</p></main></body></html>`;

for(const path of ["/api/public/availability","/availability/austin"]) {
  router.get(path,async(req,res)=>{
    res.set("Cache-Control","no-store");
    try {
      const data=await availability.search(req.query);
      if(data.next) res.set("Link",`<${path.startsWith("/api/")?data.next:data.nextHtml}>; rel="next"`);
      if(path.startsWith("/api/")) return res.json(data);
      return res.type("html").send(renderPage(data));
    } catch(error) {
      const status=error.status===400?400:503;
      if(status===503) {res.set("Retry-After","30");console.error("[PUBLIC AVAILABILITY]",error.message);}
      const message=status===400?error.message:"Availability is temporarily unavailable. Please retry; this is not a zero-availability result.";
      return res.status(status).json({success:false,error:message});
    }
  });
}
router.get("/availability-methodology",(_req,res)=>res.type("html").send(method));
router.get("/llms.txt",(_req,res)=>res.type("text/plain").send(`# NextAppt.ai\n\nAustin-area appointment inventory for configured public business services. Coverage is partial.\n\n- Readable live inventory: https://nextappt.ai/availability/austin\n- Public JSON: https://nextappt.ai/api/public/availability\n- Massage inventory: https://nextappt.ai/availability/austin?category=massage\n- Data definitions and parameters: https://nextappt.ai/availability-methodology\n\nDefault: next 24 hours, fresh confirmed entries. Follow next for remaining pages. lastCheckedAt is source observation time, not generatedAt. Inferred entries are not confirmed. Reconfirm using bookingUrl. This file is supplementary documentation, not a ranking directive.\n`));
module.exports=router;